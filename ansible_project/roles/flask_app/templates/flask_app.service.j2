--
[Unit]
Description=Flask Application
After=network.target

[Service]
User=root
Group=root
WorkingDirectory={{ deployment_location }}
Environment=”PATH={{ venv_path }}/bin”
ExecStart={{ venv_path }}/bin/flask run -p {{ app_port }}
Restart=always

[Install]
WantedBy=multi-user.target
